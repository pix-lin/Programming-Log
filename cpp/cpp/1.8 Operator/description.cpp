#include <iostream>

using namespace std;

int main()
{
	int x = 2; //x는 변수(variable), 2는 리터럴(Literal)

	cout << "Hello World" << endl;
	cout << 1 + 2 << endl; //1 + 2는 expression

	/* 리터럴(Literal) */
	//문자 그대로의 고정된 값을 표현하는 방식
	//숫자(정수, 실수 등), 문자('a'), 문자열("abc"), 불리언(true/false), 배열, 객체 등

	/* 연산자(Operator) */
	//단항(Unary) 연산자 : 피연산자가 1개
	//이항(Binary) : 피연산자가 2개
	//삼항(Ternary) : 피연산자가 3개 [?] *c++에서는 딱 하나

	/* 피연산자(Operand) */
	//연산을 당하는 대상

	/* 영역 연산자(Scope Operator) [::] */

	return 0;
}


